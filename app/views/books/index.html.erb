<h1>books</h1>

<% @books.each do |book| %>

<p>Title</p>
<span><%= book.title %></span>

<% end %>



<h4>New book</h4>

<%= form_with model: @book do |f| %>
  <h5>Title</h5>
  <%= f.text_field :title %>

  <h5>Body</h5>
  <%= f.text_area :body　%>

  <%= f.submit '投稿' %>
<% end %>